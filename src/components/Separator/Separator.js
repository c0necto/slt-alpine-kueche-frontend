import React from "react"
import * as styles from "./Separator.module.scss"

const Separator = props => {
    return (
        <div className={styles.graphicSeparation}></div>
    )
}

export default Separator
